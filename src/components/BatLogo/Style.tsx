import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  text: {
    color: "yellow",
    fontSize: 30,
    marginBottom: 10,
    textAlign: "center",
  },
  image: {
    resizeMode: "contain",
    height: 250,
  },
});
